type Query {
    
}

type Mutation {
    
}

type Subscription {

}

type Vehicle {
    id: ID!
    vehicle_ID: Int
    route: Route
    predictable: Boolean
    run: String!
    latitude: String
    longitude: String
    heading: Int
    seconds_since_report: Int
    serverTime: String
}

type Route {
    id: ID!
    name: String!
    vehicles: [Vehicle!]
}